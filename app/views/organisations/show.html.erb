<p style="color: green"><%= notice %></p>

<%= render @organisation %>

<div>
  <%= link_to "Back to organisations", organisations_path %> |
  <%= link_to "Edit this organisation", edit_organisation_path(@organisation) %> |
  <%= link_to "View Shifts", shifts_path %>


 <%= form_with(model: @organisation, local: true) do |form| %>
  <div class="field form-group">
    <%=form.number_field :user_id, id: :user_organisation_id, class:"form-control", value: @organisation.id, type: :hidden%>
  </div>
<% end %>
  <%= button_to "Destroy this organisation", @organisation, method: :delete %>
</div>
